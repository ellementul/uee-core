TODO:
  - When set provider, we deprecated onEvent function, and setup onEventForTime function:
    - Make warning for using onEvent function outside constructor 
    - Delete callback from event
    - Wrap callback with counting calls and automatic deleting
  - Make list events for listening, and check to doesn't send them from this member:
    - define via onEvent is Error
    - define via onEventForTime is Warning