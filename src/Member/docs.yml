TODO:
  - Payload can't be Array
  - Deep clone payload of event
  - When set provider, we deprecated onEvent function, and setup onEventForTime function
  - Make list events for infinite listening(define in constructor), and check to doesn't send them from this member